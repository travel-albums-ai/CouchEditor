import { createSliderNode } from '@/windows/pipeline/components/AdjustmentSliderNode';

export const BlackAndWhiteNode = createSliderNode({
  type: "black-white",
});

export const BrightnessNode = createSliderNode({
  type: "brightness",
});

export const ContrastNode = createSliderNode({
  type: "contrast",
});

export const CropNode = createSliderNode({
  type: "crop",
});

export const ExposureNode = createSliderNode({
  type: "exposure",
});

export const FadeNode = createSliderNode({
  type: "fade",
});

export const FlipNode = createSliderNode({
  type: "flip",
});

export const GammaNode = createSliderNode({
  type: "gamma",
});

export const GrainNode = createSliderNode({
  type: "grain",
});

export const HdrNode = createSliderNode({
  type: "hdr",
});

export const HighlightsNode = createSliderNode({
  type: "highlights",
});

export const HueRotationNode = createSliderNode({
  type: "hue-rotation",
});

////

export const InvertNode = createSliderNode({
  type: "invert",
});
export const LuminosityNode = createSliderNode({
  type: "luminosity",
});
export const MirrorNode = createSliderNode({
  type: "mirror",
});
export const PerspectiveNode = createSliderNode({
  type: "perspective",
});
export const PopNode = createSliderNode({
  type: "pop",
});

export const RgbBlackPointNode = createSliderNode({
  type: 'rgb-black-point',
});

export const RgbWhitePointNode = createSliderNode({
  type: 'rgb-white-point',
});

export const RgbMidtonesNode = createSliderNode({
  type: 'rgb-midtones',
});

export const RotateNode = createSliderNode({
  type: "rotate",
});

export const SaturationNode = createSliderNode({
  type: "saturation",
});

export const SepiaNode = createSliderNode({
  type: "sepia",
});

export const ShadowsNode = createSliderNode({
  type: "shadows",
});

export const SharpenNode = createSliderNode({
  type: "sharpen",
});

export const TemperatureTintNode = createSliderNode({
  type: "temperature-tint",
});

export const VibranceNode = createSliderNode({
  type: "vibrance",
});

export const WhitesBlacksNode = createSliderNode({
  type: "whites-blacks",
});
